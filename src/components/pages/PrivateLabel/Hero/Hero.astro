---
import PrivateHeroThree from "./HeroThree";
import PrivateHeroContent from "./HeroContent";
---
<section class="private-hero">
    <div class="container grid">
        <PrivateHeroContent
            {...Astro.props}
            client:visible={{ rootMargin: "100% 0px 100% 0px" }}
        />
    </div>
    <PrivateHeroThree client:idle/>
</section>
