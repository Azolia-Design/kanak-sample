---
import FullfilHero from "./Hero";
import { convertHighlight } from "@/components/utils/text";
---

<FullfilHero
    title={convertHighlight(Astro.props.title)}
    sub={Astro.props.sub[0].text}
    client:visible={{ rootMargin: "200% 0px 200% 0px" }}
/>
